    <footer id="contact" class="site-footer">
      <div class="footer-grid">
        <div>
          <h3><%= t.footer_contact || 'Contact' %></h3>
          <p>
            <a href="mailto:go@hopeturtles.org">go@hopeturtles.org</a><br />
            <span>
              <%=
                t.footer_contact_blurb ||
                  'A last mile, ecological, decentralized and open source pathway to connect humans to humans in moments of need.'
              %>
            </span>
          </p>
        </div>
        <div>
          <h3><%= t.footer_network || 'Network' %></h3>
          <ul>
            <li><a href="https://earthen.io" target="_blank" rel="noreferrer">Earthen.io</a></li>
            <li><a href="https://github.com/russs95/hopeturtles.org/" target="_blank" rel="noreferrer">GitHub</a></li>
            <li><a href="https://ecobricks.org/en/ecojoiners.php" target="_blank" rel="noreferrer">Ecojoiners</a></li>
            <li><a href="https://mapim.org.my/" target="_blank" rel="noreferrer">MAPIM</a></li>
            <li><a href="https://ecobricks.org" target="_blank" rel="noreferrer">Global Ecobrick Alliance</a></li>
          </ul>
        </div>
        <div>
          <h3><%= t.footer_regen || 'Regen Accounting' %></h3>
          <p><%= t.footer_regen_blurb || 'Hosted in Indonesia, by Dewaweb with custom in site carbon accounting..' %></p>
          <% if (includeWebsiteCarbon) { %>
            <div id="wcb" class="carbonbadge<%= theme === 'dark' ? ' wcb-d' : '' %>"></div>
            <script src="https://unpkg.com/website-carbon-badges@1.1.3/b.min.js" defer></script>
          <% } %>
        </div>
        <% if (currentUser) {
             const accountName =
               currentUser.name ||
               currentUser.firstName ||
               (currentUser.email ? currentUser.email.split('@')[0] : null) ||
               'Unknown user';
             const accountRole = currentUser.role || 'user';
             const accountLastLogin = currentUser.lastLogin
               ? new Date(currentUser.lastLogin).toLocaleString('en-US', {
                   dateStyle: 'medium',
                   timeStyle: 'short'
                 })
               : 'Never';
        %>
          <div class="footer-account">
            <h3><%= t.footer_account || 'Account' %></h3>
            <dl class="footer-account__meta">
              <div>
                <dt><%= t.footer_account_name || 'Name' %></dt>
                <dd><%= accountName %></dd>
              </div>
              <div>
                <dt><%= t.footer_account_role || 'Role' %></dt>
                <dd class="footer-account__role"><%= accountRole %></dd>
              </div>
              <div>
                <dt><%= t.footer_account_last_login || 'Last logged in' %></dt>
                <dd><%= accountLastLogin %></dd>
              </div>
            </dl>
            <a class="button ghost" href="/auth/logout"><%= t.footer_logout || 'Logout' %></a>
          </div>
        <% } %>
      </div>
      <p class="footer-meta">© <%= new Date().getFullYear() %> HopeTurtles.org · <%= t.footer_meta || 'A Human2Human project' %></p>
    </footer>
    <script src="/js/main.js" type="module"></script>
    <% if (typeof pageScripts !== 'undefined') { %>
      <% pageScripts.forEach(function (script) { %>
        <script src="<%= script %>"></script>
      <% }); %>
    <% } %>
  </body>
</html>
