<% const extraStyles = ['https://unpkg.com/leaflet@1.9.4/dist/leaflet.css']; %>
<%- include('partials/header', { pageTitle, theme, extraStyles }) %>
<%- include('partials/nav') %>
<main id="main" class="layout layout--home">
  <section class="hero">
    <div class="hero-image">
      <img
        src="/images/hopeturtles-floating-1000px.webp"
        alt="<%= t.hero_image_alt || 'Illustration of a Hope Turtle floating in the ocean' %>"
        loading="lazy"
      />
    </div>
    <div class="hero-metrics">
      <div class="metric-card">
        <span class="metric-value"><%= summary.missions %></span>
        <span class="metric-label"><%= t.metric_missions || 'Missions' %></span>
      </div>
      <div class="metric-card">
        <span class="metric-value"><%= summary.turtles %></span>
        <span class="metric-label"><%= t.metric_turtles || 'Hope Turtles' %></span>
      </div>
      <div class="metric-card">
        <span class="metric-value"><%= summary.successes %></span>
        <span class="metric-label"><%= t.metric_success || 'Successful Landfalls' %></span>
      </div>
    </div>
  </section>

  <section class="landing-intro">
    <div class="landing-intro__content">
      <h1><%= t.landing_welcome_title %></h1>
      <p><%= t.landing_intro_paragraph1 %></p>
      <p><%= t.landing_intro_paragraph2 %></p>
    </div>
  </section>

  <section class="panel contribute-panel" id="contribute">
    <header class="panel-header">
      <div>
        <h2><%= t.contribute_title || 'Contribute & Collaborate' %></h2>
        <p class="panel-subtitle">
          <%=
            t.contribute_subtitle ||
              'The HopeTurtle project is 100% open source and collaboration powered!  You can get involved in a bunch of ways.'
          %>
        </p>
      </div>
    </header>
    <div class="contribute-panel__grid">
      <% const contributeButtons = [
        { title: t.contribute_code_title || 'Code', subtitle: t.contribute_code_body || 'Help develop our turtle python code on Github' },
        { title: t.contribute_build_title || 'Build', subtitle: t.contribute_build_body || 'Assemble the hardware for a Hope Turtle' },
        { title: t.contribute_bottle_title || 'Bottle', subtitle: t.contribute_bottle_body || 'Send in food filled aid bottles to our coordination hubs' },
        {
          title: t.contribute_commission_title || 'Commission',
          subtitle: t.contribute_commission_body || 'Commission the deployment of a HopeTurtle and become its captain'
        },
        { title: t.contribute_deploy_title || 'Deploy', subtitle: t.contribute_deploy_body || 'Help deploy a hub or boat that can receive and release Hope Turtles' },
        { title: t.contribute_buy_title || 'Buy', subtitle: t.contribute_buy_body || 'Purchase food orders for our hubs to fill aid bottles' }
      ]; %>
      <% contributeButtons.forEach(function (button) { %>
        <button
          type="button"
          class="contribute-panel__button"
          data-contribute-action="<%= button.title %>"
        >
          <span class="contribute-panel__title"><%= button.title %></span>
          <span class="contribute-panel__subtitle"><%= button.subtitle %></span>
        </button>
      <% }) %>
    </div>
  </section>
</main>
<% const pageScripts = [
  'https://unpkg.com/leaflet@1.9.4/dist/leaflet.js',
  '/js/home.js'
]; %>
<%- include('partials/footer', { pageScripts }) %>
